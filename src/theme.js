import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#00843D" }, // Kelley Green
    secondary: { main: "#FFFFFF" }, // White
    text: { primary: "#000000" }, // Black
  },
});

export default theme;
